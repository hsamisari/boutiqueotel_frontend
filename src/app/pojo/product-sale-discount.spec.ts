import { ProductSaleDiscount } from './product-sale-discount';

describe('ProductSaleDiscount', () => {
  it('should create an instance', () => {
    expect(new ProductSaleDiscount()).toBeTruthy();
  });
});
